package processing.watcher;

import processing.collision.CollisionEvent;
import processing.shape.MoveShape;

public class MoveActionTransport implements ShapeAction {
	
	public void handle(CollisionEvent event) {
	}
}